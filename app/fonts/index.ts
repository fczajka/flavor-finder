import { Josefin_Sans, Literata } from 'next/font/google';

const defaultFont = Josefin_Sans({
  display: 'swap',
  weight: '400',
  variable: '--font-default',
});

const headlineFont = Literata({
  display: 'swap',
  weight: '400',
  variable: '--font-headline',
});
